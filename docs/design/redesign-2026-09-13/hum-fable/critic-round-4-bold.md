# Critic round 4 (bold pass) · branch `redesign/hum-fable` · 13 Sep 2026

The owner rejected the round-3 state as "doesn't look good" and asked for a real redesign. This pass went bold inside Hum: solid toy-physics tiles with a hard edge, the balance as a pear receipt ticket with the app's perforation and a stub that counts receipts, the frog with a speech bubble, a push send button, and a numbered rail of the four stages a task goes through. Critic: a fresh Claude Fable 5.1 session, screenshots only, same prompt as before.

## Score: 7/10 (up from a quieter 7 the critic called generic)

"The desktop view finally has a personality (the perforated pear ticket and the frog's 'Nothing needs you.' are things a person would remember)."

## Punch list and what was done

| # | Finding | Action |
| --- | --- | --- |
| 1 | Composer clips content at 1024 and 320. | Fixed: the headline owns the full row below the wide tier, tiles are title-only at the 1024 tier, the ticket is two rows on short phones. |
| 2 | Dead band above the composer; suggests moving the composer under the H1. | The rail is anchored just above the composer so the space is structured; the composer stays docked per the approved spec. Declined the move. |
| 3 | Ticket says "simulated" three times; "Unavailable" reads as an outage. | Fixed: one Simulated chip, the stub now carries a real fact (the receipt count), the state word is quiet. |
| 4 | Rail hidden below 1024; uneven copy; colour-cycled numbers. | Fixed: parallel verbs (Ask, Watch it live, Approve each spend, Keep the receipt), one disc colour, shown and wrapping from the tablet up. |
| 5 | Headline orphan at 768; bubble in the H1 band. | Fixed: the H1 spans the row until the wide tier; the frog sits beside the lede. |
| 6 | 2:1:1 tile grid; suggests three equal tiles. | Declined: three identical cards in a row is the pattern the design system bans; one big plus two small is Hum's own shape. |

Shared-chrome tells the critic named (kbd hint row, "Options") are outside this scope.
